// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: sessions.sql

package repo

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createSession = `-- name: CreateSession :one
INSERT INTO user_sessions (user_id, device_id, token_hash, expires_at)
VALUES ($1, $2, $3, $4)
RETURNING id, user_id, device_id, token_hash, expires_at, revoked_at, created_at, updated_at
`

type CreateSessionParams struct {
	UserID    int32              `json:"user_id"`
	DeviceID  string             `json:"device_id"`
	TokenHash string             `json:"token_hash"`
	ExpiresAt pgtype.Timestamptz `json:"expires_at"`
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) (UserSession, error) {
	row := q.db.QueryRow(ctx, createSession,
		arg.UserID,
		arg.DeviceID,
		arg.TokenHash,
		arg.ExpiresAt,
	)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DeviceID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteStaleSessions = `-- name: DeleteStaleSessions :execrows
DELETE FROM user_sessions
WHERE expires_at < NOW()
  OR (revoked_at IS NOT NULL AND revoked_at < NOW() - INTERVAL '30 days')
`

func (q *Queries) DeleteStaleSessions(ctx context.Context) (int64, error) {
	result, err := q.db.Exec(ctx, deleteStaleSessions)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const getActiveSessionByTokenHash = `-- name: GetActiveSessionByTokenHash :one
SELECT id, user_id, device_id, token_hash, expires_at, revoked_at, created_at, updated_at
FROM user_sessions
WHERE token_hash = $1
  AND revoked_at IS NULL
  AND expires_at > NOW()
`

func (q *Queries) GetActiveSessionByTokenHash(ctx context.Context, tokenHash string) (UserSession, error) {
	row := q.db.QueryRow(ctx, getActiveSessionByTokenHash, tokenHash)
	var i UserSession
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.DeviceID,
		&i.TokenHash,
		&i.ExpiresAt,
		&i.RevokedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const revokeSessionByTokenHash = `-- name: RevokeSessionByTokenHash :execrows
UPDATE user_sessions
SET revoked_at = NOW(),
    updated_at = CURRENT_TIMESTAMP
WHERE token_hash = $1
  AND revoked_at IS NULL
`

func (q *Queries) RevokeSessionByTokenHash(ctx context.Context, tokenHash string) (int64, error) {
	result, err := q.db.Exec(ctx, revokeSessionByTokenHash, tokenHash)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const revokeSessionsByUserAndDevice = `-- name: RevokeSessionsByUserAndDevice :execrows
UPDATE user_sessions
SET revoked_at = NOW(),
    updated_at = CURRENT_TIMESTAMP
WHERE user_id = $1
  AND device_id = $2
  AND revoked_at IS NULL
`

type RevokeSessionsByUserAndDeviceParams struct {
	UserID   int32  `json:"user_id"`
	DeviceID string `json:"device_id"`
}

func (q *Queries) RevokeSessionsByUserAndDevice(ctx context.Context, arg RevokeSessionsByUserAndDeviceParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokeSessionsByUserAndDevice, arg.UserID, arg.DeviceID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
